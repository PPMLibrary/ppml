macro add_arg(name,type,default,min,ctrl_name,help_txt)

% scope.arg name: name.to_s.to_sym, type: type.to_s.gsub!(/(\'|\")(?<tp>.*)(\'|\")/,'\k<tp>'), default: default, min: min, ctrl_name: ctrl_name, help: help_txt

end macro
