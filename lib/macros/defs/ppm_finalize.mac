macro ppm_finalize()
% scope.use :ppm_module_core
% scope.var :info, "integer"

call ppm_finalize(info)
or_fail("ppm_finalize failed.")

% if configatron.mpi
call MPI_Finalize(info)
or_fail("MPI_Finalize failed.")
% end
end macro
