client pse
  integer, dimension(4) :: bcd = ppm_param_bcdef_periodic
  integer :: istage

  ppm_init()

  t = create_topology(bcd)

  c = create_particles(t)
  global_mapping(c,t)

  f = create_field(2, "Field1")

  discretize(f,c)

  ghost_mapping(c)
  create_neighlist(c)

  ! abstract_L = define_op(2, [2, 0, 0, 2], [1, 1, 1, 1], "Laplacian")
  !   - compute_operator       (d,c)

  o = create_ode([f=>c], pse_rhs, [f], eulerf)

!  timeloop
!    do istage=1,4
!     - step_ode             (o,c)
!    end do
!  end timeloop

  ppm_finalize()
end client
