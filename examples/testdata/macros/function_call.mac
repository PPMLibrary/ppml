macro simple(arg1, arg2)
call ppm_init(info)
x = exp(<%= arg2 %>+2)
if (x .ge. 3.0_MK) then
  write(*,*) "Some Crap <%= arg1 %>"
end
end macro

macro with_ruby(arg)
! Some comment
<%
3+2
puts "Hello, World! : ", arg2
%>
end macro

macro override()
old body
end macro
